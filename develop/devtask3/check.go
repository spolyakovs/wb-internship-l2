package main

// TODO

// // checkLines checks if lines are in order (considering r)
// func checkLines(lines []string) error {
// 	values := valuesFromLines(lines)
// 	return checkLiveValues(lines, values)
// }

// func checkLiveValues(lines []string, values [][]byte) error {
// 	// if len(lines) < 1 won't go into cycle
// 	for i := 1; i < len(lines); i++ {
// 		if lessValues(values[i - 1])
// 	}
// 	return nil
// }
